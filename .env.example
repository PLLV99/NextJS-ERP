# Example environment variables for NextJS-ERP
# Copy this file to `.env.local` (for local dev) and update values.

# Public API base URL (used by client-side code)
NEXT_PUBLIC_API_URL=http://localhost:3000/api

# Server-only API key (do NOT expose this to client builds)
API_KEY=replace-with-your-secret-api-key

# Cookie key used to store auth token (client needs to know this name)
NEXT_PUBLIC_TOKEN_KEY=token_erp
